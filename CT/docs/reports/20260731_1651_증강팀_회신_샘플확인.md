# [회신] 저강도 샘플 확인 완료 — 강도 범위만 조정 후 본 생성 부탁드립니다

**보내는 곳**: CT 품질 게이트 분류기 담당
**작성**: 2026-07-31 16:51
**대상**: `C:\ct_low_signal_noise` 샘플 50장 (5종 × 10장)

---

## 결론

**샘플 이상 없습니다. 생성 방식 그대로 진행하시면 됩니다.**
다만 **강도 범위만 아래 표로 바꿔서** 본 생성을 부탁드립니다.

---

## 1. 확인 결과 — 전부 통과

| 확인 항목 | 결과 |
|---|---|
| 파라미터가 사양 밖(약한 쪽)으로 생성되는가 | ✅ 5종 전부 |
| **gate가 막지 않는가** (가장 걱정했던 부분) | ✅ 50/50 성공 |
| 해상도·형식이 기존과 같은가 | ✅ 장변 512, 동일 |
| 우리 파이프라인에 그대로 들어가는가 | ✅ 코드 수정 없이 채점 완료 |

`matching_manifest.csv`에 **`source_image_name`을 넣어 주신 점 특히 도움이 됐습니다.**
따로 요청드리지 않았는데 덕분에 원본 셀을 바로 추적할 수 있었습니다.

수량 10장·단일 셀(101)은 샘플이므로 문제 삼지 않았습니다.
오히려 한 셀로 통제해 주셔서 케이스 간 비교가 깔끔했습니다.

---

## 2. 샘플로 알아낸 것 — 감사합니다, 도움이 컸습니다

50장으로 **분류기의 검출 문턱 2개를 특정**할 수 있었습니다.

| 케이스 | 샘플 강도 | 분류기 FAIL 판정률 | 알아낸 것 |
|---|---|---|---|
| `beam_hardening` | streak 9~12 | **0%** | 사양(24~72)에서는 100%인데 여기선 전멸 → **문턱이 12~24 사이** |
| `cell_alignment` | retained 0.92~0.96 | **50%** | **전이 구간 정중앙 명중** |

또한 저희가 자체 근사 구현으로 추정했던 문턱이 **실제 생성 코드 기준으로는 약 10배 어긋나 있었다**는 것도
이 샘플로 확인했습니다. 아래 §3의 범위는 그 교정을 반영한 값입니다.

---

## 3. 본 생성 요청 — 강도 범위 교체

5종 모두 **"사양보다 한 단계 약하게"** 를 일괄 적용해 주신 것으로 보입니다(사양 대비 약 2~3배).
합리적인 해석이었으나, 케이스마다 분류기 문턱까지의 거리가 달라
**3종은 아직 문턱에 도달하지 못했습니다**(분류기가 여전히 90~100% 잡아냄).

| 케이스 | 샘플 실측 | **본 생성 요청 범위** | 사유 |
|---|---|---|---|
| `ct_low_signal_noise` | photon 95~130 | **150 ~ 3000** (로그 간격 권장) | 130에서도 90% 검출 |
| `ct_acquisition_motion` | offset 6.3~8.8 | **1 ~ 6 px** | 6.3px에서 90% 검출 |
| `ct_insufficient_projection_sampling` | retained 0.72~0.875 | **0.875 ~ 0.98** | 0.875에서 아직 100% 검출 |
| `ct_beam_hardening_metal_streak` | streak 9~12 | **12 ~ 24** | 0%와 100%를 잇는 구간 |
| `ct_cell_alignment_failure` | retained 0.92~0.96 | **0.93 ~ 0.99** | 전이 구간 해상도 확보 |

**연속 난수 방식은 그대로 유지해 주십시오.**
요청서에는 이산 고정값(3000/1000/300/100)으로 적었지만,
곡선을 그리는 데는 샘플처럼 **범위 내 연속 분포가 더 낫습니다.** 그 부분은 저희 요청이 부정확했습니다.

---

## 4. 본 생성 시 함께 부탁드릴 2가지

### 4.1 원본 셀

샘플은 battery **101**에서 나왔는데, 이 셀은 **분류기 학습에 사용한 셀**이라 측정이 편향됩니다.
본 생성은 아래 15개에서 뽑아 주십시오(최초 요청서 §2.3과 동일).

```
102, 103, 108, 114, 120, 122, 125, 134, 135, 140, 141, 143, 144, 146, 147
```

### 4.2 수량

**케이스당 50장 이상** 부탁드립니다. 10장으로는 판정률 오차가 ±15%p라 문턱을 특정할 수 없습니다.

---

## 정리

| 항목 | 변경 |
|---|---|
| 생성 방식 · 형식 · gate 설정 | **그대로** |
| 강도 범위 | **§3 표로 교체** ← 유일한 필수 변경 |
| 원본 셀 | 101 → §4.1 목록 15개 |
| 수량 | 케이스당 10 → 50 이상 |

샘플 덕분에 범위를 정확히 좁힐 수 있었습니다. 빠른 확인 감사드립니다.
문의사항 있으시면 알려주십시오.
